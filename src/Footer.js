import React from 'react';
import { Toolbar, ToolbarGroup } from 'material-ui/Toolbar';

const Footer = () => (
    <Toolbar className="footer-bkgrd-copy-2">
        <ToolbarGroup>
        </ToolbarGroup>
    </Toolbar>
);

export default Footer;
